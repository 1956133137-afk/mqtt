package com.yannuo.dgcanteen.greendao.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.yannuo.dgcanteen.greendao.entity.OfflineOrderTable;

import com.yannuo.dgcanteen.greendao.entity.OfflineDishTable;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "OFFLINE_DISH_TABLE".
*/
public class OfflineDishTableDao extends AbstractDao<OfflineDishTable, Long> {

    public static final String TABLENAME = "OFFLINE_DISH_TABLE";

    /**
     * Properties of entity OfflineDishTable.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property OfflineOrderId = new Property(1, Long.class, "offlineOrderId", false, "OFFLINE_ORDER_ID");
        public final static Property DishesId = new Property(2, String.class, "dishesId", false, "DISHES_ID");
        public final static Property DishesName = new Property(3, String.class, "dishesName", false, "DISHES_NAME");
        public final static Property DishesNumber = new Property(4, String.class, "dishesNumber", false, "DISHES_NUMBER");
        public final static Property DishesPrice = new Property(5, String.class, "dishesPrice", false, "DISHES_PRICE");
    }

    private DaoSession daoSession;

    private Query<OfflineDishTable> offlineOrderTable_PaymentDishesQuery;

    public OfflineDishTableDao(DaoConfig config) {
        super(config);
    }
    
    public OfflineDishTableDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"OFFLINE_DISH_TABLE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"OFFLINE_ORDER_ID\" INTEGER," + // 1: offlineOrderId
                "\"DISHES_ID\" TEXT," + // 2: dishesId
                "\"DISHES_NAME\" TEXT," + // 3: dishesName
                "\"DISHES_NUMBER\" TEXT," + // 4: dishesNumber
                "\"DISHES_PRICE\" TEXT);"); // 5: dishesPrice
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"OFFLINE_DISH_TABLE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, OfflineDishTable entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long offlineOrderId = entity.getOfflineOrderId();
        if (offlineOrderId != null) {
            stmt.bindLong(2, offlineOrderId);
        }
 
        String dishesId = entity.getDishesId();
        if (dishesId != null) {
            stmt.bindString(3, dishesId);
        }
 
        String dishesName = entity.getDishesName();
        if (dishesName != null) {
            stmt.bindString(4, dishesName);
        }
 
        String dishesNumber = entity.getDishesNumber();
        if (dishesNumber != null) {
            stmt.bindString(5, dishesNumber);
        }
 
        String dishesPrice = entity.getDishesPrice();
        if (dishesPrice != null) {
            stmt.bindString(6, dishesPrice);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, OfflineDishTable entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long offlineOrderId = entity.getOfflineOrderId();
        if (offlineOrderId != null) {
            stmt.bindLong(2, offlineOrderId);
        }
 
        String dishesId = entity.getDishesId();
        if (dishesId != null) {
            stmt.bindString(3, dishesId);
        }
 
        String dishesName = entity.getDishesName();
        if (dishesName != null) {
            stmt.bindString(4, dishesName);
        }
 
        String dishesNumber = entity.getDishesNumber();
        if (dishesNumber != null) {
            stmt.bindString(5, dishesNumber);
        }
 
        String dishesPrice = entity.getDishesPrice();
        if (dishesPrice != null) {
            stmt.bindString(6, dishesPrice);
        }
    }

    @Override
    protected final void attachEntity(OfflineDishTable entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public OfflineDishTable readEntity(Cursor cursor, int offset) {
        OfflineDishTable entity = new OfflineDishTable( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // offlineOrderId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // dishesId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // dishesName
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // dishesNumber
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // dishesPrice
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, OfflineDishTable entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setOfflineOrderId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setDishesId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDishesName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setDishesNumber(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setDishesPrice(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(OfflineDishTable entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(OfflineDishTable entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(OfflineDishTable entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "paymentDishes" to-many relationship of OfflineOrderTable. */
    public List<OfflineDishTable> _queryOfflineOrderTable_PaymentDishes(Long offlineOrderId) {
        synchronized (this) {
            if (offlineOrderTable_PaymentDishesQuery == null) {
                QueryBuilder<OfflineDishTable> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.OfflineOrderId.eq(null));
                offlineOrderTable_PaymentDishesQuery = queryBuilder.build();
            }
        }
        Query<OfflineDishTable> query = offlineOrderTable_PaymentDishesQuery.forCurrentThread();
        query.setParameter(0, offlineOrderId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getOfflineOrderTableDao().getAllColumns());
            builder.append(" FROM OFFLINE_DISH_TABLE T");
            builder.append(" LEFT JOIN OFFLINE_ORDER_TABLE T0 ON T.\"OFFLINE_ORDER_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected OfflineDishTable loadCurrentDeep(Cursor cursor, boolean lock) {
        OfflineDishTable entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        OfflineOrderTable offlineOrderTable = loadCurrentOther(daoSession.getOfflineOrderTableDao(), cursor, offset);
        entity.setOfflineOrderTable(offlineOrderTable);

        return entity;    
    }

    public OfflineDishTable loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<OfflineDishTable> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<OfflineDishTable> list = new ArrayList<OfflineDishTable>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<OfflineDishTable> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<OfflineDishTable> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
