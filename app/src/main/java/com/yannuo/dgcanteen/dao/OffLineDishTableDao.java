package com.yannuo.dgcanteen.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "OFF_LINE_DISH_TABLE".
*/
public class OffLineDishTableDao extends AbstractDao<OffLineDishTable, Long> {

    public static final String TABLENAME = "OFF_LINE_DISH_TABLE";

    /**
     * Properties of entity OffLineDishTable.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CorDishId = new Property(1, Long.class, "corDishId", false, "COR_DISH_ID");
        public final static Property DishesId = new Property(2, String.class, "dishesId", false, "DISHES_ID");
        public final static Property DishesName = new Property(3, String.class, "dishesName", false, "DISHES_NAME");
        public final static Property DishesNumber = new Property(4, int.class, "dishesNumber", false, "DISHES_NUMBER");
        public final static Property DishesPrice = new Property(5, double.class, "dishesPrice", false, "DISHES_PRICE");
    }

    private DaoSession daoSession;

    private Query<OffLineDishTable> offLineTable_OffLineDishesListQuery;

    public OffLineDishTableDao(DaoConfig config) {
        super(config);
    }
    
    public OffLineDishTableDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"OFF_LINE_DISH_TABLE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"COR_DISH_ID\" INTEGER," + // 1: corDishId
                "\"DISHES_ID\" TEXT," + // 2: dishesId
                "\"DISHES_NAME\" TEXT," + // 3: dishesName
                "\"DISHES_NUMBER\" INTEGER NOT NULL ," + // 4: dishesNumber
                "\"DISHES_PRICE\" REAL NOT NULL );"); // 5: dishesPrice
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"OFF_LINE_DISH_TABLE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, OffLineDishTable entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long corDishId = entity.getCorDishId();
        if (corDishId != null) {
            stmt.bindLong(2, corDishId);
        }
 
        String dishesId = entity.getDishesId();
        if (dishesId != null) {
            stmt.bindString(3, dishesId);
        }
 
        String dishesName = entity.getDishesName();
        if (dishesName != null) {
            stmt.bindString(4, dishesName);
        }
        stmt.bindLong(5, entity.getDishesNumber());
        stmt.bindDouble(6, entity.getDishesPrice());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, OffLineDishTable entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long corDishId = entity.getCorDishId();
        if (corDishId != null) {
            stmt.bindLong(2, corDishId);
        }
 
        String dishesId = entity.getDishesId();
        if (dishesId != null) {
            stmt.bindString(3, dishesId);
        }
 
        String dishesName = entity.getDishesName();
        if (dishesName != null) {
            stmt.bindString(4, dishesName);
        }
        stmt.bindLong(5, entity.getDishesNumber());
        stmt.bindDouble(6, entity.getDishesPrice());
    }

    @Override
    protected final void attachEntity(OffLineDishTable entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public OffLineDishTable readEntity(Cursor cursor, int offset) {
        OffLineDishTable entity = new OffLineDishTable( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // corDishId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // dishesId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // dishesName
            cursor.getInt(offset + 4), // dishesNumber
            cursor.getDouble(offset + 5) // dishesPrice
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, OffLineDishTable entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCorDishId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setDishesId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDishesName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setDishesNumber(cursor.getInt(offset + 4));
        entity.setDishesPrice(cursor.getDouble(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(OffLineDishTable entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(OffLineDishTable entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(OffLineDishTable entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "offLineDishesList" to-many relationship of OffLineTable. */
    public List<OffLineDishTable> _queryOffLineTable_OffLineDishesList(Long corDishId) {
        synchronized (this) {
            if (offLineTable_OffLineDishesListQuery == null) {
                QueryBuilder<OffLineDishTable> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.CorDishId.eq(null));
                offLineTable_OffLineDishesListQuery = queryBuilder.build();
            }
        }
        Query<OffLineDishTable> query = offLineTable_OffLineDishesListQuery.forCurrentThread();
        query.setParameter(0, corDishId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getOffLineTableDao().getAllColumns());
            builder.append(" FROM OFF_LINE_DISH_TABLE T");
            builder.append(" LEFT JOIN OFF_LINE_TABLE T0 ON T.\"COR_DISH_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected OffLineDishTable loadCurrentDeep(Cursor cursor, boolean lock) {
        OffLineDishTable entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        OffLineTable corDish = loadCurrentOther(daoSession.getOffLineTableDao(), cursor, offset);
        entity.setCorDish(corDish);

        return entity;    
    }

    public OffLineDishTable loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<OffLineDishTable> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<OffLineDishTable> list = new ArrayList<OffLineDishTable>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<OffLineDishTable> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<OffLineDishTable> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
